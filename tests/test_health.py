# Dev dependencies are listed in requirements.txt (python-multipart is needed for future /api/conversation)
# health.py uses requests which is a dependency of uvicorn[standard] or fastapi, but not explicitly declared
